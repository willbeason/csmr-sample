apiVersion: configsync.gke.io/v1alpha1
kind: RepoSync
metadata:
  name: repo-sync
  namespace: bookstore
spec:
  git:
    repo: https://github.com/willbeason/csmr-sample.git
    dir: "bookstore"
    auth: none
